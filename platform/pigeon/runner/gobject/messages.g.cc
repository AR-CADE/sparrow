// Copyright 2013 The Flutter Authors. All rights reserved.
// Use of this source code is governed by a BSD-style license that can be
// found in the LICENSE file.
// Autogenerated from Pigeon (v28.1.0), do not edit directly.
// See also: https://pub.dev/packages/pigeon

#include <cmath>

#include <string.h>
#include "messages.g.h"
static guint G_GNUC_UNUSED flpigeon_hash_double(double v) {
  if (std::isnan(v)) {
    return static_cast<guint>(0x7FF80000);
  }
  if (v == 0.0) {
    v = 0.0;
  }
  union { double d; uint64_t u; } u;
  u.d = v;
  return static_cast<guint>(u.u ^ (u.u >> 32));
}
static gboolean G_GNUC_UNUSED flpigeon_equals_double(double a, double b) {
  return (a == b) || (std::isnan(a) && std::isnan(b));
}
static gboolean G_GNUC_UNUSED flpigeon_deep_equals(FlValue* a, FlValue* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (fl_value_get_type(a) != fl_value_get_type(b)) {
    return FALSE;
  }
  switch (fl_value_get_type(a)) {
    case FL_VALUE_TYPE_NULL:
      return TRUE;
    case FL_VALUE_TYPE_BOOL:
      return fl_value_get_bool(a) == fl_value_get_bool(b);
    case FL_VALUE_TYPE_INT:
      return fl_value_get_int(a) == fl_value_get_int(b);
    case FL_VALUE_TYPE_FLOAT: {
      return flpigeon_equals_double(fl_value_get_float(a), fl_value_get_float(b));
    }
    case FL_VALUE_TYPE_STRING:
      return g_strcmp0(fl_value_get_string(a), fl_value_get_string(b)) == 0;
    case FL_VALUE_TYPE_UINT8_LIST:
      return fl_value_get_length(a) == fl_value_get_length(b) &&
             memcmp(fl_value_get_uint8_list(a), fl_value_get_uint8_list(b), fl_value_get_length(a)) == 0;
    case FL_VALUE_TYPE_INT32_LIST:
      return fl_value_get_length(a) == fl_value_get_length(b) &&
             memcmp(fl_value_get_int32_list(a), fl_value_get_int32_list(b), fl_value_get_length(a) * sizeof(int32_t)) == 0;
    case FL_VALUE_TYPE_INT64_LIST:
      return fl_value_get_length(a) == fl_value_get_length(b) &&
             memcmp(fl_value_get_int64_list(a), fl_value_get_int64_list(b), fl_value_get_length(a) * sizeof(int64_t)) == 0;
    case FL_VALUE_TYPE_FLOAT_LIST: {
      size_t len = fl_value_get_length(a);
      if (len != fl_value_get_length(b)) {
        return FALSE;
      }
      const double* a_data = fl_value_get_float_list(a);
      const double* b_data = fl_value_get_float_list(b);
      for (size_t i = 0; i < len; i++) {
      if (!flpigeon_equals_double(a_data[i], b_data[i])) {
        return FALSE;
      }
    }
      return TRUE;
    }
    case FL_VALUE_TYPE_LIST: {
      size_t len = fl_value_get_length(a);
      if (len != fl_value_get_length(b)) {
        return FALSE;
      }
      for (size_t i = 0; i < len; i++) {
      if (!flpigeon_deep_equals(fl_value_get_list_value(a, i), fl_value_get_list_value(b, i))) {
        return FALSE;
      }
    }
      return TRUE;
    }
    case FL_VALUE_TYPE_MAP: {
      size_t len = fl_value_get_length(a);
      if (len != fl_value_get_length(b)) {
        return FALSE;
      }
      for (size_t i = 0; i < len; i++) {
      FlValue* key = fl_value_get_map_key(a, i);
      FlValue* val = fl_value_get_map_value(a, i);
      gboolean found = FALSE;
      for (size_t j = 0; j < len; j++) {
        FlValue* b_key = fl_value_get_map_key(b, j);
        if (flpigeon_deep_equals(key, b_key)) {
          FlValue* b_val = fl_value_get_map_value(b, j);
          if (flpigeon_deep_equals(val, b_val)) {
            found = TRUE;
            break;
          } else {
            return FALSE;
          }
        }
      }
      if (!found) {
        return FALSE;
      }
    }
      return TRUE;
    }
    default:
      return FALSE;
  }
  return FALSE;
}
static guint G_GNUC_UNUSED flpigeon_deep_hash(FlValue* value) {
  if (value == nullptr) {
    return 0;
  }
  switch (fl_value_get_type(value)) {
    case FL_VALUE_TYPE_NULL:
      return 0;
    case FL_VALUE_TYPE_BOOL:
      return fl_value_get_bool(value) ? 1231 : 1237;
    case FL_VALUE_TYPE_INT: {
      int64_t v = fl_value_get_int(value);
      return static_cast<guint>(v ^ (v >> 32));
    }
    case FL_VALUE_TYPE_FLOAT:
      return flpigeon_hash_double(fl_value_get_float(value));
    case FL_VALUE_TYPE_STRING:
      return g_str_hash(fl_value_get_string(value));
    case FL_VALUE_TYPE_UINT8_LIST: {
      guint result = 1;
      size_t len = fl_value_get_length(value);
      const uint8_t* data = fl_value_get_uint8_list(value);
      for (size_t i = 0; i < len; i++) {
        result = result * 31 + data[i];
      }
      return result;
    }
    case FL_VALUE_TYPE_INT32_LIST: {
      guint result = 1;
      size_t len = fl_value_get_length(value);
      const int32_t* data = fl_value_get_int32_list(value);
      for (size_t i = 0; i < len; i++) {
        result = result * 31 + static_cast<guint>(data[i]);
      }
      return result;
    }
    case FL_VALUE_TYPE_INT64_LIST: {
      guint result = 1;
      size_t len = fl_value_get_length(value);
      const int64_t* data = fl_value_get_int64_list(value);
      for (size_t i = 0; i < len; i++) {
        result = result * 31 + static_cast<guint>(data[i] ^ (data[i] >> 32));
      }
      return result;
    }
    case FL_VALUE_TYPE_FLOAT_LIST: {
      guint result = 1;
      size_t len = fl_value_get_length(value);
      const double* data = fl_value_get_float_list(value);
      for (size_t i = 0; i < len; i++) {
      result = result * 31 + flpigeon_hash_double(data[i]);
    }
      return result;
    }
    case FL_VALUE_TYPE_LIST: {
      guint result = 1;
      size_t len = fl_value_get_length(value);
      for (size_t i = 0; i < len; i++) {
      result = result * 31 + flpigeon_deep_hash(fl_value_get_list_value(value, i));
    }
      return result;
    }
    case FL_VALUE_TYPE_MAP: {
      guint result = 0;
      size_t len = fl_value_get_length(value);
      for (size_t i = 0; i < len; i++) {
      result += ((flpigeon_deep_hash(fl_value_get_map_key(value, i)) * 31) ^ flpigeon_deep_hash(fl_value_get_map_value(value, i)));
    }
      return result;
    }
    default:
      return static_cast<guint>(fl_value_get_type(value));
  }
  return 0;
}
static gchar* G_GNUC_UNUSED flpigeon_to_string(FlValue* value) {
  if (value == nullptr) {
    return g_strdup("null");
  }
  switch (fl_value_get_type(value)) {
    case FL_VALUE_TYPE_NULL:
      return g_strdup("null");
    case FL_VALUE_TYPE_BOOL:
      return g_strdup(fl_value_get_bool(value) ? "true" : "false");
    case FL_VALUE_TYPE_INT:
      return g_strdup_printf("%" G_GINT64_FORMAT, fl_value_get_int(value));
    case FL_VALUE_TYPE_FLOAT:
      return g_strdup_printf("%g", fl_value_get_float(value));
    case FL_VALUE_TYPE_STRING:
      return g_strdup_printf("\"%s\"", fl_value_get_string(value));
    case FL_VALUE_TYPE_UINT8_LIST: {
      GString* str = g_string_new("[");
      size_t len = fl_value_get_length(value);
      const uint8_t* data = fl_value_get_uint8_list(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        g_string_append_printf(str, "%d", data[i]);
      }
      g_string_append(str, "]");
      return g_string_free(str, FALSE);
    }
    case FL_VALUE_TYPE_INT32_LIST: {
      GString* str = g_string_new("[");
      size_t len = fl_value_get_length(value);
      const int32_t* data = fl_value_get_int32_list(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        g_string_append_printf(str, "%d", data[i]);
      }
      g_string_append(str, "]");
      return g_string_free(str, FALSE);
    }
    case FL_VALUE_TYPE_INT64_LIST: {
      GString* str = g_string_new("[");
      size_t len = fl_value_get_length(value);
      const int64_t* data = fl_value_get_int64_list(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        g_string_append_printf(str, "%" G_GINT64_FORMAT, data[i]);
      }
      g_string_append(str, "]");
      return g_string_free(str, FALSE);
    }
    case FL_VALUE_TYPE_FLOAT_LIST: {
      GString* str = g_string_new("[");
      size_t len = fl_value_get_length(value);
      const double* data = fl_value_get_float_list(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        g_string_append_printf(str, "%g", data[i]);
      }
      g_string_append(str, "]");
      return g_string_free(str, FALSE);
    }
    case FL_VALUE_TYPE_LIST: {
      GString* str = g_string_new("[");
      size_t len = fl_value_get_length(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        gchar* item_str = flpigeon_to_string(fl_value_get_list_value(value, i));
        g_string_append(str, item_str);
        g_free(item_str);
      }
      g_string_append(str, "]");
      return g_string_free(str, FALSE);
    }
    case FL_VALUE_TYPE_MAP: {
      GString* str = g_string_new("{");
      size_t len = fl_value_get_length(value);
      for (size_t i = 0; i < len; i++) {
        if (i > 0) {
          g_string_append(str, ", ");
        }
        gchar* key_str = flpigeon_to_string(fl_value_get_map_key(value, i));
        gchar* val_str = flpigeon_to_string(fl_value_get_map_value(value, i));
        g_string_append_printf(str, "%s: %s", key_str, val_str);
        g_free(key_str);
        g_free(val_str);
      }
      g_string_append(str, "}");
      return g_string_free(str, FALSE);
    }
    default:
      return g_strdup("[custom]");
  }
  return g_strdup("null");
}


struct _PigeonRunnerRunnerPongData {
  GObject parent_instance;

  gboolean pong;
  gchar* timestamp_ms;
};

G_DEFINE_TYPE(PigeonRunnerRunnerPongData, pigeon_runner_runner_pong_data, G_TYPE_OBJECT)

static void pigeon_runner_runner_pong_data_dispose(GObject* object) {
  PigeonRunnerRunnerPongData* self = PIGEON_RUNNER_RUNNER_PONG_DATA(object);
  g_clear_pointer(&self->timestamp_ms, g_free);
  G_OBJECT_CLASS(pigeon_runner_runner_pong_data_parent_class)->dispose(object);
}

static void pigeon_runner_runner_pong_data_init(PigeonRunnerRunnerPongData* self) {
}

static void pigeon_runner_runner_pong_data_class_init(PigeonRunnerRunnerPongDataClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_pong_data_dispose;
}

PigeonRunnerRunnerPongData* pigeon_runner_runner_pong_data_new(gboolean pong, const gchar* timestamp_ms) {
  PigeonRunnerRunnerPongData* self = PIGEON_RUNNER_RUNNER_PONG_DATA(g_object_new(pigeon_runner_runner_pong_data_get_type(), nullptr));
  self->pong = pong;
  self->timestamp_ms = g_strdup(timestamp_ms);
  return self;
}

gboolean pigeon_runner_runner_pong_data_get_pong(PigeonRunnerRunnerPongData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_PONG_DATA(self), FALSE);
  return self->pong;
}

const gchar* pigeon_runner_runner_pong_data_get_timestamp_ms(PigeonRunnerRunnerPongData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_PONG_DATA(self), nullptr);
  return self->timestamp_ms;
}

static FlValue* pigeon_runner_runner_pong_data_to_list(PigeonRunnerRunnerPongData* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, fl_value_new_bool(self->pong));
  fl_value_append_take(values, fl_value_new_string(self->timestamp_ms));
  return values;
}

static PigeonRunnerRunnerPongData* pigeon_runner_runner_pong_data_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  gboolean pong = fl_value_get_bool(value0);
  FlValue* value1 = fl_value_get_list_value(values, 1);
  const gchar* timestamp_ms = fl_value_get_string(value1);
  return pigeon_runner_runner_pong_data_new(pong, timestamp_ms);
}

gboolean pigeon_runner_runner_pong_data_equals(PigeonRunnerRunnerPongData* a, PigeonRunnerRunnerPongData* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (a->pong != b->pong) {
    return FALSE;
  }
  if (g_strcmp0(a->timestamp_ms, b->timestamp_ms) != 0) {
    return FALSE;
  }
  return TRUE;
}

guint pigeon_runner_runner_pong_data_hash(PigeonRunnerRunnerPongData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_PONG_DATA(self), 0);
  guint result = 0;
  result = result * 31 + static_cast<guint>(self->pong);
  result = result * 31 + (self->timestamp_ms != nullptr ? g_str_hash(self->timestamp_ms) : 0);
  return result;
}

gchar* pigeon_runner_runner_pong_data_to_string(PigeonRunnerRunnerPongData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_PONG_DATA(self), NULL);
  GString* str = g_string_new("RunnerPongData(");
  g_string_append(str, "pong: ");
  g_string_append(str, self->pong ? "true" : "false");
  g_string_append(str, ", timestamp_ms: ");
  if (self->timestamp_ms != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->timestamp_ms);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _PigeonRunnerRunnerSystemInfoData {
  GObject parent_instance;

  gchar* runner;
  gchar* version;
  gchar* wayland_display;
  int64_t width;
  int64_t height;
  double pixel_ratio;
  gboolean fullscreen;
  gboolean maximized;
  gchar* title;
  gchar* app_id;
};

G_DEFINE_TYPE(PigeonRunnerRunnerSystemInfoData, pigeon_runner_runner_system_info_data, G_TYPE_OBJECT)

static void pigeon_runner_runner_system_info_data_dispose(GObject* object) {
  PigeonRunnerRunnerSystemInfoData* self = PIGEON_RUNNER_RUNNER_SYSTEM_INFO_DATA(object);
  g_clear_pointer(&self->runner, g_free);
  g_clear_pointer(&self->version, g_free);
  g_clear_pointer(&self->wayland_display, g_free);
  g_clear_pointer(&self->title, g_free);
  g_clear_pointer(&self->app_id, g_free);
  G_OBJECT_CLASS(pigeon_runner_runner_system_info_data_parent_class)->dispose(object);
}

static void pigeon_runner_runner_system_info_data_init(PigeonRunnerRunnerSystemInfoData* self) {
}

static void pigeon_runner_runner_system_info_data_class_init(PigeonRunnerRunnerSystemInfoDataClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_system_info_data_dispose;
}

PigeonRunnerRunnerSystemInfoData* pigeon_runner_runner_system_info_data_new(const gchar* runner, const gchar* version, const gchar* wayland_display, int64_t width, int64_t height, double pixel_ratio, gboolean fullscreen, gboolean maximized, const gchar* title, const gchar* app_id) {
  PigeonRunnerRunnerSystemInfoData* self = PIGEON_RUNNER_RUNNER_SYSTEM_INFO_DATA(g_object_new(pigeon_runner_runner_system_info_data_get_type(), nullptr));
  self->runner = g_strdup(runner);
  self->version = g_strdup(version);
  self->wayland_display = g_strdup(wayland_display);
  self->width = width;
  self->height = height;
  self->pixel_ratio = pixel_ratio;
  self->fullscreen = fullscreen;
  self->maximized = maximized;
  self->title = g_strdup(title);
  self->app_id = g_strdup(app_id);
  return self;
}

const gchar* pigeon_runner_runner_system_info_data_get_runner(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), nullptr);
  return self->runner;
}

const gchar* pigeon_runner_runner_system_info_data_get_version(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), nullptr);
  return self->version;
}

const gchar* pigeon_runner_runner_system_info_data_get_wayland_display(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), nullptr);
  return self->wayland_display;
}

int64_t pigeon_runner_runner_system_info_data_get_width(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), 0);
  return self->width;
}

int64_t pigeon_runner_runner_system_info_data_get_height(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), 0);
  return self->height;
}

double pigeon_runner_runner_system_info_data_get_pixel_ratio(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), 0.0);
  return self->pixel_ratio;
}

gboolean pigeon_runner_runner_system_info_data_get_fullscreen(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), FALSE);
  return self->fullscreen;
}

gboolean pigeon_runner_runner_system_info_data_get_maximized(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), FALSE);
  return self->maximized;
}

const gchar* pigeon_runner_runner_system_info_data_get_title(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), nullptr);
  return self->title;
}

const gchar* pigeon_runner_runner_system_info_data_get_app_id(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), nullptr);
  return self->app_id;
}

static FlValue* pigeon_runner_runner_system_info_data_to_list(PigeonRunnerRunnerSystemInfoData* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, fl_value_new_string(self->runner));
  fl_value_append_take(values, fl_value_new_string(self->version));
  fl_value_append_take(values, fl_value_new_string(self->wayland_display));
  fl_value_append_take(values, fl_value_new_int(self->width));
  fl_value_append_take(values, fl_value_new_int(self->height));
  fl_value_append_take(values, fl_value_new_float(self->pixel_ratio));
  fl_value_append_take(values, fl_value_new_bool(self->fullscreen));
  fl_value_append_take(values, fl_value_new_bool(self->maximized));
  fl_value_append_take(values, fl_value_new_string(self->title));
  fl_value_append_take(values, fl_value_new_string(self->app_id));
  return values;
}

static PigeonRunnerRunnerSystemInfoData* pigeon_runner_runner_system_info_data_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  const gchar* runner = fl_value_get_string(value0);
  FlValue* value1 = fl_value_get_list_value(values, 1);
  const gchar* version = fl_value_get_string(value1);
  FlValue* value2 = fl_value_get_list_value(values, 2);
  const gchar* wayland_display = fl_value_get_string(value2);
  FlValue* value3 = fl_value_get_list_value(values, 3);
  int64_t width = fl_value_get_int(value3);
  FlValue* value4 = fl_value_get_list_value(values, 4);
  int64_t height = fl_value_get_int(value4);
  FlValue* value5 = fl_value_get_list_value(values, 5);
  double pixel_ratio = fl_value_get_float(value5);
  FlValue* value6 = fl_value_get_list_value(values, 6);
  gboolean fullscreen = fl_value_get_bool(value6);
  FlValue* value7 = fl_value_get_list_value(values, 7);
  gboolean maximized = fl_value_get_bool(value7);
  FlValue* value8 = fl_value_get_list_value(values, 8);
  const gchar* title = fl_value_get_string(value8);
  FlValue* value9 = fl_value_get_list_value(values, 9);
  const gchar* app_id = fl_value_get_string(value9);
  return pigeon_runner_runner_system_info_data_new(runner, version, wayland_display, width, height, pixel_ratio, fullscreen, maximized, title, app_id);
}

gboolean pigeon_runner_runner_system_info_data_equals(PigeonRunnerRunnerSystemInfoData* a, PigeonRunnerRunnerSystemInfoData* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (g_strcmp0(a->runner, b->runner) != 0) {
    return FALSE;
  }
  if (g_strcmp0(a->version, b->version) != 0) {
    return FALSE;
  }
  if (g_strcmp0(a->wayland_display, b->wayland_display) != 0) {
    return FALSE;
  }
  if (a->width != b->width) {
    return FALSE;
  }
  if (a->height != b->height) {
    return FALSE;
  }
  if (!flpigeon_equals_double(a->pixel_ratio, b->pixel_ratio)) {
    return FALSE;
  }
  if (a->fullscreen != b->fullscreen) {
    return FALSE;
  }
  if (a->maximized != b->maximized) {
    return FALSE;
  }
  if (g_strcmp0(a->title, b->title) != 0) {
    return FALSE;
  }
  if (g_strcmp0(a->app_id, b->app_id) != 0) {
    return FALSE;
  }
  return TRUE;
}

guint pigeon_runner_runner_system_info_data_hash(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), 0);
  guint result = 0;
  result = result * 31 + (self->runner != nullptr ? g_str_hash(self->runner) : 0);
  result = result * 31 + (self->version != nullptr ? g_str_hash(self->version) : 0);
  result = result * 31 + (self->wayland_display != nullptr ? g_str_hash(self->wayland_display) : 0);
  result = result * 31 + static_cast<guint>(self->width);
  result = result * 31 + static_cast<guint>(self->height);
  result = result * 31 + flpigeon_hash_double(self->pixel_ratio);
  result = result * 31 + static_cast<guint>(self->fullscreen);
  result = result * 31 + static_cast<guint>(self->maximized);
  result = result * 31 + (self->title != nullptr ? g_str_hash(self->title) : 0);
  result = result * 31 + (self->app_id != nullptr ? g_str_hash(self->app_id) : 0);
  return result;
}

gchar* pigeon_runner_runner_system_info_data_to_string(PigeonRunnerRunnerSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_SYSTEM_INFO_DATA(self), NULL);
  GString* str = g_string_new("RunnerSystemInfoData(");
  g_string_append(str, "runner: ");
  if (self->runner != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->runner);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", version: ");
  if (self->version != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->version);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", wayland_display: ");
  if (self->wayland_display != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->wayland_display);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", width: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->width);
  g_string_append(str, ", height: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->height);
  g_string_append(str, ", pixel_ratio: ");
  g_string_append_printf(str, "%g", self->pixel_ratio);
  g_string_append(str, ", fullscreen: ");
  g_string_append(str, self->fullscreen ? "true" : "false");
  g_string_append(str, ", maximized: ");
  g_string_append(str, self->maximized ? "true" : "false");
  g_string_append(str, ", title: ");
  if (self->title != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->title);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", app_id: ");
  if (self->app_id != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->app_id);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _PigeonRunnerCompositorPongData {
  GObject parent_instance;

  gboolean pong;
  int64_t timestamp_us;
  int64_t peer_pid;
};

G_DEFINE_TYPE(PigeonRunnerCompositorPongData, pigeon_runner_compositor_pong_data, G_TYPE_OBJECT)

static void pigeon_runner_compositor_pong_data_dispose(GObject* object) {
  G_OBJECT_CLASS(pigeon_runner_compositor_pong_data_parent_class)->dispose(object);
}

static void pigeon_runner_compositor_pong_data_init(PigeonRunnerCompositorPongData* self) {
}

static void pigeon_runner_compositor_pong_data_class_init(PigeonRunnerCompositorPongDataClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_compositor_pong_data_dispose;
}

PigeonRunnerCompositorPongData* pigeon_runner_compositor_pong_data_new(gboolean pong, int64_t timestamp_us, int64_t peer_pid) {
  PigeonRunnerCompositorPongData* self = PIGEON_RUNNER_COMPOSITOR_PONG_DATA(g_object_new(pigeon_runner_compositor_pong_data_get_type(), nullptr));
  self->pong = pong;
  self->timestamp_us = timestamp_us;
  self->peer_pid = peer_pid;
  return self;
}

gboolean pigeon_runner_compositor_pong_data_get_pong(PigeonRunnerCompositorPongData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_PONG_DATA(self), FALSE);
  return self->pong;
}

int64_t pigeon_runner_compositor_pong_data_get_timestamp_us(PigeonRunnerCompositorPongData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_PONG_DATA(self), 0);
  return self->timestamp_us;
}

int64_t pigeon_runner_compositor_pong_data_get_peer_pid(PigeonRunnerCompositorPongData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_PONG_DATA(self), 0);
  return self->peer_pid;
}

static FlValue* pigeon_runner_compositor_pong_data_to_list(PigeonRunnerCompositorPongData* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, fl_value_new_bool(self->pong));
  fl_value_append_take(values, fl_value_new_int(self->timestamp_us));
  fl_value_append_take(values, fl_value_new_int(self->peer_pid));
  return values;
}

static PigeonRunnerCompositorPongData* pigeon_runner_compositor_pong_data_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  gboolean pong = fl_value_get_bool(value0);
  FlValue* value1 = fl_value_get_list_value(values, 1);
  int64_t timestamp_us = fl_value_get_int(value1);
  FlValue* value2 = fl_value_get_list_value(values, 2);
  int64_t peer_pid = fl_value_get_int(value2);
  return pigeon_runner_compositor_pong_data_new(pong, timestamp_us, peer_pid);
}

gboolean pigeon_runner_compositor_pong_data_equals(PigeonRunnerCompositorPongData* a, PigeonRunnerCompositorPongData* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (a->pong != b->pong) {
    return FALSE;
  }
  if (a->timestamp_us != b->timestamp_us) {
    return FALSE;
  }
  if (a->peer_pid != b->peer_pid) {
    return FALSE;
  }
  return TRUE;
}

guint pigeon_runner_compositor_pong_data_hash(PigeonRunnerCompositorPongData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_PONG_DATA(self), 0);
  guint result = 0;
  result = result * 31 + static_cast<guint>(self->pong);
  result = result * 31 + static_cast<guint>(self->timestamp_us);
  result = result * 31 + static_cast<guint>(self->peer_pid);
  return result;
}

gchar* pigeon_runner_compositor_pong_data_to_string(PigeonRunnerCompositorPongData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_PONG_DATA(self), NULL);
  GString* str = g_string_new("CompositorPongData(");
  g_string_append(str, "pong: ");
  g_string_append(str, self->pong ? "true" : "false");
  g_string_append(str, ", timestamp_us: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->timestamp_us);
  g_string_append(str, ", peer_pid: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->peer_pid);
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _PigeonRunnerCompositorSystemInfoData {
  GObject parent_instance;

  gchar* compositor;
  gchar* version;
  gchar* ipc_channel;
  int64_t surfaces_count;
  double client_fps;
  int64_t peer_pid;
  gchar* app_id;
};

G_DEFINE_TYPE(PigeonRunnerCompositorSystemInfoData, pigeon_runner_compositor_system_info_data, G_TYPE_OBJECT)

static void pigeon_runner_compositor_system_info_data_dispose(GObject* object) {
  PigeonRunnerCompositorSystemInfoData* self = PIGEON_RUNNER_COMPOSITOR_SYSTEM_INFO_DATA(object);
  g_clear_pointer(&self->compositor, g_free);
  g_clear_pointer(&self->version, g_free);
  g_clear_pointer(&self->ipc_channel, g_free);
  g_clear_pointer(&self->app_id, g_free);
  G_OBJECT_CLASS(pigeon_runner_compositor_system_info_data_parent_class)->dispose(object);
}

static void pigeon_runner_compositor_system_info_data_init(PigeonRunnerCompositorSystemInfoData* self) {
}

static void pigeon_runner_compositor_system_info_data_class_init(PigeonRunnerCompositorSystemInfoDataClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_compositor_system_info_data_dispose;
}

PigeonRunnerCompositorSystemInfoData* pigeon_runner_compositor_system_info_data_new(const gchar* compositor, const gchar* version, const gchar* ipc_channel, int64_t surfaces_count, double client_fps, int64_t peer_pid, const gchar* app_id) {
  PigeonRunnerCompositorSystemInfoData* self = PIGEON_RUNNER_COMPOSITOR_SYSTEM_INFO_DATA(g_object_new(pigeon_runner_compositor_system_info_data_get_type(), nullptr));
  self->compositor = g_strdup(compositor);
  self->version = g_strdup(version);
  self->ipc_channel = g_strdup(ipc_channel);
  self->surfaces_count = surfaces_count;
  self->client_fps = client_fps;
  self->peer_pid = peer_pid;
  self->app_id = g_strdup(app_id);
  return self;
}

const gchar* pigeon_runner_compositor_system_info_data_get_compositor(PigeonRunnerCompositorSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SYSTEM_INFO_DATA(self), nullptr);
  return self->compositor;
}

const gchar* pigeon_runner_compositor_system_info_data_get_version(PigeonRunnerCompositorSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SYSTEM_INFO_DATA(self), nullptr);
  return self->version;
}

const gchar* pigeon_runner_compositor_system_info_data_get_ipc_channel(PigeonRunnerCompositorSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SYSTEM_INFO_DATA(self), nullptr);
  return self->ipc_channel;
}

int64_t pigeon_runner_compositor_system_info_data_get_surfaces_count(PigeonRunnerCompositorSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SYSTEM_INFO_DATA(self), 0);
  return self->surfaces_count;
}

double pigeon_runner_compositor_system_info_data_get_client_fps(PigeonRunnerCompositorSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SYSTEM_INFO_DATA(self), 0.0);
  return self->client_fps;
}

int64_t pigeon_runner_compositor_system_info_data_get_peer_pid(PigeonRunnerCompositorSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SYSTEM_INFO_DATA(self), 0);
  return self->peer_pid;
}

const gchar* pigeon_runner_compositor_system_info_data_get_app_id(PigeonRunnerCompositorSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SYSTEM_INFO_DATA(self), nullptr);
  return self->app_id;
}

static FlValue* pigeon_runner_compositor_system_info_data_to_list(PigeonRunnerCompositorSystemInfoData* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, fl_value_new_string(self->compositor));
  fl_value_append_take(values, fl_value_new_string(self->version));
  fl_value_append_take(values, fl_value_new_string(self->ipc_channel));
  fl_value_append_take(values, fl_value_new_int(self->surfaces_count));
  fl_value_append_take(values, fl_value_new_float(self->client_fps));
  fl_value_append_take(values, fl_value_new_int(self->peer_pid));
  fl_value_append_take(values, fl_value_new_string(self->app_id));
  return values;
}

static PigeonRunnerCompositorSystemInfoData* pigeon_runner_compositor_system_info_data_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  const gchar* compositor = fl_value_get_string(value0);
  FlValue* value1 = fl_value_get_list_value(values, 1);
  const gchar* version = fl_value_get_string(value1);
  FlValue* value2 = fl_value_get_list_value(values, 2);
  const gchar* ipc_channel = fl_value_get_string(value2);
  FlValue* value3 = fl_value_get_list_value(values, 3);
  int64_t surfaces_count = fl_value_get_int(value3);
  FlValue* value4 = fl_value_get_list_value(values, 4);
  double client_fps = fl_value_get_float(value4);
  FlValue* value5 = fl_value_get_list_value(values, 5);
  int64_t peer_pid = fl_value_get_int(value5);
  FlValue* value6 = fl_value_get_list_value(values, 6);
  const gchar* app_id = fl_value_get_string(value6);
  return pigeon_runner_compositor_system_info_data_new(compositor, version, ipc_channel, surfaces_count, client_fps, peer_pid, app_id);
}

gboolean pigeon_runner_compositor_system_info_data_equals(PigeonRunnerCompositorSystemInfoData* a, PigeonRunnerCompositorSystemInfoData* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (g_strcmp0(a->compositor, b->compositor) != 0) {
    return FALSE;
  }
  if (g_strcmp0(a->version, b->version) != 0) {
    return FALSE;
  }
  if (g_strcmp0(a->ipc_channel, b->ipc_channel) != 0) {
    return FALSE;
  }
  if (a->surfaces_count != b->surfaces_count) {
    return FALSE;
  }
  if (!flpigeon_equals_double(a->client_fps, b->client_fps)) {
    return FALSE;
  }
  if (a->peer_pid != b->peer_pid) {
    return FALSE;
  }
  if (g_strcmp0(a->app_id, b->app_id) != 0) {
    return FALSE;
  }
  return TRUE;
}

guint pigeon_runner_compositor_system_info_data_hash(PigeonRunnerCompositorSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SYSTEM_INFO_DATA(self), 0);
  guint result = 0;
  result = result * 31 + (self->compositor != nullptr ? g_str_hash(self->compositor) : 0);
  result = result * 31 + (self->version != nullptr ? g_str_hash(self->version) : 0);
  result = result * 31 + (self->ipc_channel != nullptr ? g_str_hash(self->ipc_channel) : 0);
  result = result * 31 + static_cast<guint>(self->surfaces_count);
  result = result * 31 + flpigeon_hash_double(self->client_fps);
  result = result * 31 + static_cast<guint>(self->peer_pid);
  result = result * 31 + (self->app_id != nullptr ? g_str_hash(self->app_id) : 0);
  return result;
}

gchar* pigeon_runner_compositor_system_info_data_to_string(PigeonRunnerCompositorSystemInfoData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SYSTEM_INFO_DATA(self), NULL);
  GString* str = g_string_new("CompositorSystemInfoData(");
  g_string_append(str, "compositor: ");
  if (self->compositor != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->compositor);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", version: ");
  if (self->version != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->version);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", ipc_channel: ");
  if (self->ipc_channel != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->ipc_channel);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", surfaces_count: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->surfaces_count);
  g_string_append(str, ", client_fps: ");
  g_string_append_printf(str, "%g", self->client_fps);
  g_string_append(str, ", peer_pid: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->peer_pid);
  g_string_append(str, ", app_id: ");
  if (self->app_id != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->app_id);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _PigeonRunnerCompositorSurfaceData {
  GObject parent_instance;

  gchar* title;
  int64_t handle;
  gchar* app_id;
  int64_t width;
  int64_t height;
  gboolean fullscreen;
  gboolean maximized;
  gboolean activated;
};

G_DEFINE_TYPE(PigeonRunnerCompositorSurfaceData, pigeon_runner_compositor_surface_data, G_TYPE_OBJECT)

static void pigeon_runner_compositor_surface_data_dispose(GObject* object) {
  PigeonRunnerCompositorSurfaceData* self = PIGEON_RUNNER_COMPOSITOR_SURFACE_DATA(object);
  g_clear_pointer(&self->title, g_free);
  g_clear_pointer(&self->app_id, g_free);
  G_OBJECT_CLASS(pigeon_runner_compositor_surface_data_parent_class)->dispose(object);
}

static void pigeon_runner_compositor_surface_data_init(PigeonRunnerCompositorSurfaceData* self) {
}

static void pigeon_runner_compositor_surface_data_class_init(PigeonRunnerCompositorSurfaceDataClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_compositor_surface_data_dispose;
}

PigeonRunnerCompositorSurfaceData* pigeon_runner_compositor_surface_data_new(const gchar* title, int64_t handle, const gchar* app_id, int64_t width, int64_t height, gboolean fullscreen, gboolean maximized, gboolean activated) {
  PigeonRunnerCompositorSurfaceData* self = PIGEON_RUNNER_COMPOSITOR_SURFACE_DATA(g_object_new(pigeon_runner_compositor_surface_data_get_type(), nullptr));
  self->title = g_strdup(title);
  self->handle = handle;
  self->app_id = g_strdup(app_id);
  self->width = width;
  self->height = height;
  self->fullscreen = fullscreen;
  self->maximized = maximized;
  self->activated = activated;
  return self;
}

const gchar* pigeon_runner_compositor_surface_data_get_title(PigeonRunnerCompositorSurfaceData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SURFACE_DATA(self), nullptr);
  return self->title;
}

int64_t pigeon_runner_compositor_surface_data_get_handle(PigeonRunnerCompositorSurfaceData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SURFACE_DATA(self), 0);
  return self->handle;
}

const gchar* pigeon_runner_compositor_surface_data_get_app_id(PigeonRunnerCompositorSurfaceData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SURFACE_DATA(self), nullptr);
  return self->app_id;
}

int64_t pigeon_runner_compositor_surface_data_get_width(PigeonRunnerCompositorSurfaceData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SURFACE_DATA(self), 0);
  return self->width;
}

int64_t pigeon_runner_compositor_surface_data_get_height(PigeonRunnerCompositorSurfaceData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SURFACE_DATA(self), 0);
  return self->height;
}

gboolean pigeon_runner_compositor_surface_data_get_fullscreen(PigeonRunnerCompositorSurfaceData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SURFACE_DATA(self), FALSE);
  return self->fullscreen;
}

gboolean pigeon_runner_compositor_surface_data_get_maximized(PigeonRunnerCompositorSurfaceData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SURFACE_DATA(self), FALSE);
  return self->maximized;
}

gboolean pigeon_runner_compositor_surface_data_get_activated(PigeonRunnerCompositorSurfaceData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SURFACE_DATA(self), FALSE);
  return self->activated;
}

static FlValue* pigeon_runner_compositor_surface_data_to_list(PigeonRunnerCompositorSurfaceData* self) {
  FlValue* values = fl_value_new_list();
  fl_value_append_take(values, fl_value_new_string(self->title));
  fl_value_append_take(values, fl_value_new_int(self->handle));
  fl_value_append_take(values, fl_value_new_string(self->app_id));
  fl_value_append_take(values, fl_value_new_int(self->width));
  fl_value_append_take(values, fl_value_new_int(self->height));
  fl_value_append_take(values, fl_value_new_bool(self->fullscreen));
  fl_value_append_take(values, fl_value_new_bool(self->maximized));
  fl_value_append_take(values, fl_value_new_bool(self->activated));
  return values;
}

static PigeonRunnerCompositorSurfaceData* pigeon_runner_compositor_surface_data_new_from_list(FlValue* values) {
  FlValue* value0 = fl_value_get_list_value(values, 0);
  const gchar* title = fl_value_get_string(value0);
  FlValue* value1 = fl_value_get_list_value(values, 1);
  int64_t handle = fl_value_get_int(value1);
  FlValue* value2 = fl_value_get_list_value(values, 2);
  const gchar* app_id = fl_value_get_string(value2);
  FlValue* value3 = fl_value_get_list_value(values, 3);
  int64_t width = fl_value_get_int(value3);
  FlValue* value4 = fl_value_get_list_value(values, 4);
  int64_t height = fl_value_get_int(value4);
  FlValue* value5 = fl_value_get_list_value(values, 5);
  gboolean fullscreen = fl_value_get_bool(value5);
  FlValue* value6 = fl_value_get_list_value(values, 6);
  gboolean maximized = fl_value_get_bool(value6);
  FlValue* value7 = fl_value_get_list_value(values, 7);
  gboolean activated = fl_value_get_bool(value7);
  return pigeon_runner_compositor_surface_data_new(title, handle, app_id, width, height, fullscreen, maximized, activated);
}

gboolean pigeon_runner_compositor_surface_data_equals(PigeonRunnerCompositorSurfaceData* a, PigeonRunnerCompositorSurfaceData* b) {
  if (a == b) {
    return TRUE;
  }
  if (a == nullptr || b == nullptr) {
    return FALSE;
  }
  if (g_strcmp0(a->title, b->title) != 0) {
    return FALSE;
  }
  if (a->handle != b->handle) {
    return FALSE;
  }
  if (g_strcmp0(a->app_id, b->app_id) != 0) {
    return FALSE;
  }
  if (a->width != b->width) {
    return FALSE;
  }
  if (a->height != b->height) {
    return FALSE;
  }
  if (a->fullscreen != b->fullscreen) {
    return FALSE;
  }
  if (a->maximized != b->maximized) {
    return FALSE;
  }
  if (a->activated != b->activated) {
    return FALSE;
  }
  return TRUE;
}

guint pigeon_runner_compositor_surface_data_hash(PigeonRunnerCompositorSurfaceData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SURFACE_DATA(self), 0);
  guint result = 0;
  result = result * 31 + (self->title != nullptr ? g_str_hash(self->title) : 0);
  result = result * 31 + static_cast<guint>(self->handle);
  result = result * 31 + (self->app_id != nullptr ? g_str_hash(self->app_id) : 0);
  result = result * 31 + static_cast<guint>(self->width);
  result = result * 31 + static_cast<guint>(self->height);
  result = result * 31 + static_cast<guint>(self->fullscreen);
  result = result * 31 + static_cast<guint>(self->maximized);
  result = result * 31 + static_cast<guint>(self->activated);
  return result;
}

gchar* pigeon_runner_compositor_surface_data_to_string(PigeonRunnerCompositorSurfaceData* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_COMPOSITOR_SURFACE_DATA(self), NULL);
  GString* str = g_string_new("CompositorSurfaceData(");
  g_string_append(str, "title: ");
  if (self->title != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->title);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", handle: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->handle);
  g_string_append(str, ", app_id: ");
  if (self->app_id != nullptr) {
    g_string_append_printf(str, "\"%s\"", self->app_id);
  }
  else {
    g_string_append(str, "null");
  }
  g_string_append(str, ", width: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->width);
  g_string_append(str, ", height: ");
  g_string_append_printf(str, "%" G_GINT64_FORMAT, self->height);
  g_string_append(str, ", fullscreen: ");
  g_string_append(str, self->fullscreen ? "true" : "false");
  g_string_append(str, ", maximized: ");
  g_string_append(str, self->maximized ? "true" : "false");
  g_string_append(str, ", activated: ");
  g_string_append(str, self->activated ? "true" : "false");
  g_string_append(str, ")");
  return g_string_free(str, FALSE);
}

struct _PigeonRunnerMessageCodec {
  FlStandardMessageCodec parent_instance;

};

G_DEFINE_TYPE(PigeonRunnerMessageCodec, pigeon_runner_message_codec, fl_standard_message_codec_get_type())

const int pigeon_runner_runner_pong_data_type_id = 129;
const int pigeon_runner_runner_system_info_data_type_id = 130;
const int pigeon_runner_compositor_pong_data_type_id = 131;
const int pigeon_runner_compositor_system_info_data_type_id = 132;
const int pigeon_runner_compositor_surface_data_type_id = 133;

static gboolean pigeon_runner_message_codec_write_pigeon_runner_runner_pong_data(FlStandardMessageCodec* codec, GByteArray* buffer, PigeonRunnerRunnerPongData* value, GError** error) {
  uint8_t type = pigeon_runner_runner_pong_data_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = pigeon_runner_runner_pong_data_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean pigeon_runner_message_codec_write_pigeon_runner_runner_system_info_data(FlStandardMessageCodec* codec, GByteArray* buffer, PigeonRunnerRunnerSystemInfoData* value, GError** error) {
  uint8_t type = pigeon_runner_runner_system_info_data_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = pigeon_runner_runner_system_info_data_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean pigeon_runner_message_codec_write_pigeon_runner_compositor_pong_data(FlStandardMessageCodec* codec, GByteArray* buffer, PigeonRunnerCompositorPongData* value, GError** error) {
  uint8_t type = pigeon_runner_compositor_pong_data_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = pigeon_runner_compositor_pong_data_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean pigeon_runner_message_codec_write_pigeon_runner_compositor_system_info_data(FlStandardMessageCodec* codec, GByteArray* buffer, PigeonRunnerCompositorSystemInfoData* value, GError** error) {
  uint8_t type = pigeon_runner_compositor_system_info_data_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = pigeon_runner_compositor_system_info_data_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean pigeon_runner_message_codec_write_pigeon_runner_compositor_surface_data(FlStandardMessageCodec* codec, GByteArray* buffer, PigeonRunnerCompositorSurfaceData* value, GError** error) {
  uint8_t type = pigeon_runner_compositor_surface_data_type_id;
  g_byte_array_append(buffer, &type, sizeof(uint8_t));
  g_autoptr(FlValue) values = pigeon_runner_compositor_surface_data_to_list(value);
  return fl_standard_message_codec_write_value(codec, buffer, values, error);
}

static gboolean pigeon_runner_message_codec_write_value(FlStandardMessageCodec* codec, GByteArray* buffer, FlValue* value, GError** error) {
  if (fl_value_get_type(value) == FL_VALUE_TYPE_CUSTOM) {
    switch (fl_value_get_custom_type(value)) {
      case pigeon_runner_runner_pong_data_type_id:
        return pigeon_runner_message_codec_write_pigeon_runner_runner_pong_data(codec, buffer, PIGEON_RUNNER_RUNNER_PONG_DATA(fl_value_get_custom_value_object(value)), error);
      case pigeon_runner_runner_system_info_data_type_id:
        return pigeon_runner_message_codec_write_pigeon_runner_runner_system_info_data(codec, buffer, PIGEON_RUNNER_RUNNER_SYSTEM_INFO_DATA(fl_value_get_custom_value_object(value)), error);
      case pigeon_runner_compositor_pong_data_type_id:
        return pigeon_runner_message_codec_write_pigeon_runner_compositor_pong_data(codec, buffer, PIGEON_RUNNER_COMPOSITOR_PONG_DATA(fl_value_get_custom_value_object(value)), error);
      case pigeon_runner_compositor_system_info_data_type_id:
        return pigeon_runner_message_codec_write_pigeon_runner_compositor_system_info_data(codec, buffer, PIGEON_RUNNER_COMPOSITOR_SYSTEM_INFO_DATA(fl_value_get_custom_value_object(value)), error);
      case pigeon_runner_compositor_surface_data_type_id:
        return pigeon_runner_message_codec_write_pigeon_runner_compositor_surface_data(codec, buffer, PIGEON_RUNNER_COMPOSITOR_SURFACE_DATA(fl_value_get_custom_value_object(value)), error);
    }
  }

  return FL_STANDARD_MESSAGE_CODEC_CLASS(pigeon_runner_message_codec_parent_class)->write_value(codec, buffer, value, error);
}

static FlValue* pigeon_runner_message_codec_read_pigeon_runner_runner_pong_data(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(PigeonRunnerRunnerPongData) value = pigeon_runner_runner_pong_data_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(pigeon_runner_runner_pong_data_type_id, G_OBJECT(value));
}

static FlValue* pigeon_runner_message_codec_read_pigeon_runner_runner_system_info_data(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(PigeonRunnerRunnerSystemInfoData) value = pigeon_runner_runner_system_info_data_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(pigeon_runner_runner_system_info_data_type_id, G_OBJECT(value));
}

static FlValue* pigeon_runner_message_codec_read_pigeon_runner_compositor_pong_data(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(PigeonRunnerCompositorPongData) value = pigeon_runner_compositor_pong_data_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(pigeon_runner_compositor_pong_data_type_id, G_OBJECT(value));
}

static FlValue* pigeon_runner_message_codec_read_pigeon_runner_compositor_system_info_data(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(PigeonRunnerCompositorSystemInfoData) value = pigeon_runner_compositor_system_info_data_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(pigeon_runner_compositor_system_info_data_type_id, G_OBJECT(value));
}

static FlValue* pigeon_runner_message_codec_read_pigeon_runner_compositor_surface_data(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, GError** error) {
  g_autoptr(FlValue) values = fl_standard_message_codec_read_value(codec, buffer, offset, error);
  if (values == nullptr) {
    return nullptr;
  }

  g_autoptr(PigeonRunnerCompositorSurfaceData) value = pigeon_runner_compositor_surface_data_new_from_list(values);
  if (value == nullptr) {
    g_set_error(error, FL_MESSAGE_CODEC_ERROR, FL_MESSAGE_CODEC_ERROR_FAILED, "Invalid data received for MessageData");
    return nullptr;
  }

  return fl_value_new_custom_object(pigeon_runner_compositor_surface_data_type_id, G_OBJECT(value));
}

static FlValue* pigeon_runner_message_codec_read_value_of_type(FlStandardMessageCodec* codec, GBytes* buffer, size_t* offset, int type, GError** error) {
  switch (type) {
    case pigeon_runner_runner_pong_data_type_id:
      return pigeon_runner_message_codec_read_pigeon_runner_runner_pong_data(codec, buffer, offset, error);
    case pigeon_runner_runner_system_info_data_type_id:
      return pigeon_runner_message_codec_read_pigeon_runner_runner_system_info_data(codec, buffer, offset, error);
    case pigeon_runner_compositor_pong_data_type_id:
      return pigeon_runner_message_codec_read_pigeon_runner_compositor_pong_data(codec, buffer, offset, error);
    case pigeon_runner_compositor_system_info_data_type_id:
      return pigeon_runner_message_codec_read_pigeon_runner_compositor_system_info_data(codec, buffer, offset, error);
    case pigeon_runner_compositor_surface_data_type_id:
      return pigeon_runner_message_codec_read_pigeon_runner_compositor_surface_data(codec, buffer, offset, error);
    default:
      return FL_STANDARD_MESSAGE_CODEC_CLASS(pigeon_runner_message_codec_parent_class)->read_value_of_type(codec, buffer, offset, type, error);
  }
}

static void pigeon_runner_message_codec_init(PigeonRunnerMessageCodec* self) {
}

static void pigeon_runner_message_codec_class_init(PigeonRunnerMessageCodecClass* klass) {
  FL_STANDARD_MESSAGE_CODEC_CLASS(klass)->write_value = pigeon_runner_message_codec_write_value;
  FL_STANDARD_MESSAGE_CODEC_CLASS(klass)->read_value_of_type = pigeon_runner_message_codec_read_value_of_type;
}

static PigeonRunnerMessageCodec* pigeon_runner_message_codec_new() {
  PigeonRunnerMessageCodec* self = PIGEON_RUNNER_MESSAGE_CODEC(g_object_new(pigeon_runner_message_codec_get_type(), nullptr));
  return self;
}

struct _PigeonRunnerRunnerHostApiResponseHandle {
  GObject parent_instance;

  FlBasicMessageChannel* channel;
  FlBasicMessageChannelResponseHandle* response_handle;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostApiResponseHandle, pigeon_runner_runner_host_api_response_handle, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_api_response_handle_dispose(GObject* object) {
  PigeonRunnerRunnerHostApiResponseHandle* self = PIGEON_RUNNER_RUNNER_HOST_API_RESPONSE_HANDLE(object);
  g_clear_object(&self->channel);
  g_clear_object(&self->response_handle);
  G_OBJECT_CLASS(pigeon_runner_runner_host_api_response_handle_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_api_response_handle_init(PigeonRunnerRunnerHostApiResponseHandle* self) {
}

static void pigeon_runner_runner_host_api_response_handle_class_init(PigeonRunnerRunnerHostApiResponseHandleClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_api_response_handle_dispose;
}

static PigeonRunnerRunnerHostApiResponseHandle* pigeon_runner_runner_host_api_response_handle_new(FlBasicMessageChannel* channel, FlBasicMessageChannelResponseHandle* response_handle) {
  PigeonRunnerRunnerHostApiResponseHandle* self = PIGEON_RUNNER_RUNNER_HOST_API_RESPONSE_HANDLE(g_object_new(pigeon_runner_runner_host_api_response_handle_get_type(), nullptr));
  self->channel = FL_BASIC_MESSAGE_CHANNEL(g_object_ref(channel));
  self->response_handle = FL_BASIC_MESSAGE_CHANNEL_RESPONSE_HANDLE(g_object_ref(response_handle));
  return self;
}

G_DECLARE_FINAL_TYPE(PigeonRunnerRunnerHostApiGetSystemInfoResponse, pigeon_runner_runner_host_api_get_system_info_response, PIGEON_RUNNER, RUNNER_HOST_API_GET_SYSTEM_INFO_RESPONSE, GObject)

struct _PigeonRunnerRunnerHostApiGetSystemInfoResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostApiGetSystemInfoResponse, pigeon_runner_runner_host_api_get_system_info_response, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_api_get_system_info_response_dispose(GObject* object) {
  PigeonRunnerRunnerHostApiGetSystemInfoResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_GET_SYSTEM_INFO_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(pigeon_runner_runner_host_api_get_system_info_response_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_api_get_system_info_response_init(PigeonRunnerRunnerHostApiGetSystemInfoResponse* self) {
}

static void pigeon_runner_runner_host_api_get_system_info_response_class_init(PigeonRunnerRunnerHostApiGetSystemInfoResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_api_get_system_info_response_dispose;
}

static PigeonRunnerRunnerHostApiGetSystemInfoResponse* pigeon_runner_runner_host_api_get_system_info_response_new(PigeonRunnerRunnerSystemInfoData* return_value) {
  PigeonRunnerRunnerHostApiGetSystemInfoResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_GET_SYSTEM_INFO_RESPONSE(g_object_new(pigeon_runner_runner_host_api_get_system_info_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, return_value != nullptr ? fl_value_new_custom_object(pigeon_runner_runner_system_info_data_type_id, G_OBJECT(return_value)) : fl_value_new_null());
  return self;
}

static PigeonRunnerRunnerHostApiGetSystemInfoResponse* pigeon_runner_runner_host_api_get_system_info_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  PigeonRunnerRunnerHostApiGetSystemInfoResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_GET_SYSTEM_INFO_RESPONSE(g_object_new(pigeon_runner_runner_host_api_get_system_info_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

G_DECLARE_FINAL_TYPE(PigeonRunnerRunnerHostApiSetWindowTitleResponse, pigeon_runner_runner_host_api_set_window_title_response, PIGEON_RUNNER, RUNNER_HOST_API_SET_WINDOW_TITLE_RESPONSE, GObject)

struct _PigeonRunnerRunnerHostApiSetWindowTitleResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostApiSetWindowTitleResponse, pigeon_runner_runner_host_api_set_window_title_response, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_api_set_window_title_response_dispose(GObject* object) {
  PigeonRunnerRunnerHostApiSetWindowTitleResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_SET_WINDOW_TITLE_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(pigeon_runner_runner_host_api_set_window_title_response_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_api_set_window_title_response_init(PigeonRunnerRunnerHostApiSetWindowTitleResponse* self) {
}

static void pigeon_runner_runner_host_api_set_window_title_response_class_init(PigeonRunnerRunnerHostApiSetWindowTitleResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_api_set_window_title_response_dispose;
}

static PigeonRunnerRunnerHostApiSetWindowTitleResponse* pigeon_runner_runner_host_api_set_window_title_response_new(gboolean return_value) {
  PigeonRunnerRunnerHostApiSetWindowTitleResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_SET_WINDOW_TITLE_RESPONSE(g_object_new(pigeon_runner_runner_host_api_set_window_title_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_bool(return_value));
  return self;
}

static PigeonRunnerRunnerHostApiSetWindowTitleResponse* pigeon_runner_runner_host_api_set_window_title_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  PigeonRunnerRunnerHostApiSetWindowTitleResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_SET_WINDOW_TITLE_RESPONSE(g_object_new(pigeon_runner_runner_host_api_set_window_title_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

G_DECLARE_FINAL_TYPE(PigeonRunnerRunnerHostApiSetFullscreenResponse, pigeon_runner_runner_host_api_set_fullscreen_response, PIGEON_RUNNER, RUNNER_HOST_API_SET_FULLSCREEN_RESPONSE, GObject)

struct _PigeonRunnerRunnerHostApiSetFullscreenResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostApiSetFullscreenResponse, pigeon_runner_runner_host_api_set_fullscreen_response, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_api_set_fullscreen_response_dispose(GObject* object) {
  PigeonRunnerRunnerHostApiSetFullscreenResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_SET_FULLSCREEN_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(pigeon_runner_runner_host_api_set_fullscreen_response_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_api_set_fullscreen_response_init(PigeonRunnerRunnerHostApiSetFullscreenResponse* self) {
}

static void pigeon_runner_runner_host_api_set_fullscreen_response_class_init(PigeonRunnerRunnerHostApiSetFullscreenResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_api_set_fullscreen_response_dispose;
}

static PigeonRunnerRunnerHostApiSetFullscreenResponse* pigeon_runner_runner_host_api_set_fullscreen_response_new(gboolean return_value) {
  PigeonRunnerRunnerHostApiSetFullscreenResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_SET_FULLSCREEN_RESPONSE(g_object_new(pigeon_runner_runner_host_api_set_fullscreen_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_bool(return_value));
  return self;
}

static PigeonRunnerRunnerHostApiSetFullscreenResponse* pigeon_runner_runner_host_api_set_fullscreen_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  PigeonRunnerRunnerHostApiSetFullscreenResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_SET_FULLSCREEN_RESPONSE(g_object_new(pigeon_runner_runner_host_api_set_fullscreen_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

G_DECLARE_FINAL_TYPE(PigeonRunnerRunnerHostApiSetMaximizedResponse, pigeon_runner_runner_host_api_set_maximized_response, PIGEON_RUNNER, RUNNER_HOST_API_SET_MAXIMIZED_RESPONSE, GObject)

struct _PigeonRunnerRunnerHostApiSetMaximizedResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostApiSetMaximizedResponse, pigeon_runner_runner_host_api_set_maximized_response, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_api_set_maximized_response_dispose(GObject* object) {
  PigeonRunnerRunnerHostApiSetMaximizedResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_SET_MAXIMIZED_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(pigeon_runner_runner_host_api_set_maximized_response_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_api_set_maximized_response_init(PigeonRunnerRunnerHostApiSetMaximizedResponse* self) {
}

static void pigeon_runner_runner_host_api_set_maximized_response_class_init(PigeonRunnerRunnerHostApiSetMaximizedResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_api_set_maximized_response_dispose;
}

static PigeonRunnerRunnerHostApiSetMaximizedResponse* pigeon_runner_runner_host_api_set_maximized_response_new(gboolean return_value) {
  PigeonRunnerRunnerHostApiSetMaximizedResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_SET_MAXIMIZED_RESPONSE(g_object_new(pigeon_runner_runner_host_api_set_maximized_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_bool(return_value));
  return self;
}

static PigeonRunnerRunnerHostApiSetMaximizedResponse* pigeon_runner_runner_host_api_set_maximized_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  PigeonRunnerRunnerHostApiSetMaximizedResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_SET_MAXIMIZED_RESPONSE(g_object_new(pigeon_runner_runner_host_api_set_maximized_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

G_DECLARE_FINAL_TYPE(PigeonRunnerRunnerHostApiMinimizeResponse, pigeon_runner_runner_host_api_minimize_response, PIGEON_RUNNER, RUNNER_HOST_API_MINIMIZE_RESPONSE, GObject)

struct _PigeonRunnerRunnerHostApiMinimizeResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostApiMinimizeResponse, pigeon_runner_runner_host_api_minimize_response, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_api_minimize_response_dispose(GObject* object) {
  PigeonRunnerRunnerHostApiMinimizeResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_MINIMIZE_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(pigeon_runner_runner_host_api_minimize_response_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_api_minimize_response_init(PigeonRunnerRunnerHostApiMinimizeResponse* self) {
}

static void pigeon_runner_runner_host_api_minimize_response_class_init(PigeonRunnerRunnerHostApiMinimizeResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_api_minimize_response_dispose;
}

static PigeonRunnerRunnerHostApiMinimizeResponse* pigeon_runner_runner_host_api_minimize_response_new(gboolean return_value) {
  PigeonRunnerRunnerHostApiMinimizeResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_MINIMIZE_RESPONSE(g_object_new(pigeon_runner_runner_host_api_minimize_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_bool(return_value));
  return self;
}

static PigeonRunnerRunnerHostApiMinimizeResponse* pigeon_runner_runner_host_api_minimize_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  PigeonRunnerRunnerHostApiMinimizeResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_MINIMIZE_RESPONSE(g_object_new(pigeon_runner_runner_host_api_minimize_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

G_DECLARE_FINAL_TYPE(PigeonRunnerRunnerHostApiPingResponse, pigeon_runner_runner_host_api_ping_response, PIGEON_RUNNER, RUNNER_HOST_API_PING_RESPONSE, GObject)

struct _PigeonRunnerRunnerHostApiPingResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostApiPingResponse, pigeon_runner_runner_host_api_ping_response, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_api_ping_response_dispose(GObject* object) {
  PigeonRunnerRunnerHostApiPingResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_PING_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(pigeon_runner_runner_host_api_ping_response_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_api_ping_response_init(PigeonRunnerRunnerHostApiPingResponse* self) {
}

static void pigeon_runner_runner_host_api_ping_response_class_init(PigeonRunnerRunnerHostApiPingResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_api_ping_response_dispose;
}

static PigeonRunnerRunnerHostApiPingResponse* pigeon_runner_runner_host_api_ping_response_new(PigeonRunnerRunnerPongData* return_value) {
  PigeonRunnerRunnerHostApiPingResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_PING_RESPONSE(g_object_new(pigeon_runner_runner_host_api_ping_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_custom_object(pigeon_runner_runner_pong_data_type_id, G_OBJECT(return_value)));
  return self;
}

static PigeonRunnerRunnerHostApiPingResponse* pigeon_runner_runner_host_api_ping_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  PigeonRunnerRunnerHostApiPingResponse* self = PIGEON_RUNNER_RUNNER_HOST_API_PING_RESPONSE(g_object_new(pigeon_runner_runner_host_api_ping_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _PigeonRunnerRunnerHostApi {
  GObject parent_instance;

  const PigeonRunnerRunnerHostApiVTable* vtable;
  gpointer user_data;
  GDestroyNotify user_data_free_func;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostApi, pigeon_runner_runner_host_api, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_api_dispose(GObject* object) {
  PigeonRunnerRunnerHostApi* self = PIGEON_RUNNER_RUNNER_HOST_API(object);
  if (self->user_data != nullptr) {
    self->user_data_free_func(self->user_data);
  }
  self->user_data = nullptr;
  G_OBJECT_CLASS(pigeon_runner_runner_host_api_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_api_init(PigeonRunnerRunnerHostApi* self) {
}

static void pigeon_runner_runner_host_api_class_init(PigeonRunnerRunnerHostApiClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_api_dispose;
}

static PigeonRunnerRunnerHostApi* pigeon_runner_runner_host_api_new(const PigeonRunnerRunnerHostApiVTable* vtable, gpointer user_data, GDestroyNotify user_data_free_func) {
  PigeonRunnerRunnerHostApi* self = PIGEON_RUNNER_RUNNER_HOST_API(g_object_new(pigeon_runner_runner_host_api_get_type(), nullptr));
  self->vtable = vtable;
  self->user_data = user_data;
  self->user_data_free_func = user_data_free_func;
  return self;
}

static void pigeon_runner_runner_host_api_get_system_info_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  PigeonRunnerRunnerHostApi* self = PIGEON_RUNNER_RUNNER_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->get_system_info == nullptr) {
    return;
  }

  g_autoptr(PigeonRunnerRunnerHostApiResponseHandle) handle = pigeon_runner_runner_host_api_response_handle_new(channel, response_handle);
  self->vtable->get_system_info(handle, self->user_data);
}

static void pigeon_runner_runner_host_api_set_window_title_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  PigeonRunnerRunnerHostApi* self = PIGEON_RUNNER_RUNNER_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_window_title == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  const gchar* title = fl_value_get_string(value0);
  g_autoptr(PigeonRunnerRunnerHostApiResponseHandle) handle = pigeon_runner_runner_host_api_response_handle_new(channel, response_handle);
  self->vtable->set_window_title(title, handle, self->user_data);
}

static void pigeon_runner_runner_host_api_set_fullscreen_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  PigeonRunnerRunnerHostApi* self = PIGEON_RUNNER_RUNNER_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_fullscreen == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  gboolean enabled = fl_value_get_bool(value0);
  g_autoptr(PigeonRunnerRunnerHostApiResponseHandle) handle = pigeon_runner_runner_host_api_response_handle_new(channel, response_handle);
  self->vtable->set_fullscreen(enabled, handle, self->user_data);
}

static void pigeon_runner_runner_host_api_set_maximized_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  PigeonRunnerRunnerHostApi* self = PIGEON_RUNNER_RUNNER_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->set_maximized == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  gboolean enabled = fl_value_get_bool(value0);
  g_autoptr(PigeonRunnerRunnerHostApiResponseHandle) handle = pigeon_runner_runner_host_api_response_handle_new(channel, response_handle);
  self->vtable->set_maximized(enabled, handle, self->user_data);
}

static void pigeon_runner_runner_host_api_minimize_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  PigeonRunnerRunnerHostApi* self = PIGEON_RUNNER_RUNNER_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->minimize == nullptr) {
    return;
  }

  g_autoptr(PigeonRunnerRunnerHostApiResponseHandle) handle = pigeon_runner_runner_host_api_response_handle_new(channel, response_handle);
  self->vtable->minimize(handle, self->user_data);
}

static void pigeon_runner_runner_host_api_ping_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  PigeonRunnerRunnerHostApi* self = PIGEON_RUNNER_RUNNER_HOST_API(user_data);

  if (self->vtable == nullptr || self->vtable->ping == nullptr) {
    return;
  }

  g_autoptr(PigeonRunnerRunnerHostApiResponseHandle) handle = pigeon_runner_runner_host_api_response_handle_new(channel, response_handle);
  self->vtable->ping(handle, self->user_data);
}

void pigeon_runner_runner_host_api_set_method_handlers(FlBinaryMessenger* messenger, const gchar* suffix, const PigeonRunnerRunnerHostApiVTable* vtable, gpointer user_data, GDestroyNotify user_data_free_func) {
  g_autofree gchar* dot_suffix = suffix != nullptr ? g_strdup_printf(".%s", suffix) : g_strdup("");
  g_autoptr(PigeonRunnerRunnerHostApi) api_data = pigeon_runner_runner_host_api_new(vtable, user_data, user_data_free_func);

  g_autoptr(PigeonRunnerMessageCodec) codec = pigeon_runner_message_codec_new();
  g_autofree gchar* get_system_info_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.getSystemInfo%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) get_system_info_channel = fl_basic_message_channel_new(messenger, get_system_info_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(get_system_info_channel, pigeon_runner_runner_host_api_get_system_info_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_window_title_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.setWindowTitle%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_window_title_channel = fl_basic_message_channel_new(messenger, set_window_title_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_window_title_channel, pigeon_runner_runner_host_api_set_window_title_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_fullscreen_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.setFullscreen%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_fullscreen_channel = fl_basic_message_channel_new(messenger, set_fullscreen_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_fullscreen_channel, pigeon_runner_runner_host_api_set_fullscreen_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* set_maximized_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.setMaximized%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_maximized_channel = fl_basic_message_channel_new(messenger, set_maximized_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_maximized_channel, pigeon_runner_runner_host_api_set_maximized_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* minimize_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.minimize%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) minimize_channel = fl_basic_message_channel_new(messenger, minimize_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(minimize_channel, pigeon_runner_runner_host_api_minimize_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* ping_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.ping%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) ping_channel = fl_basic_message_channel_new(messenger, ping_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(ping_channel, pigeon_runner_runner_host_api_ping_cb, g_object_ref(api_data), g_object_unref);
}

void pigeon_runner_runner_host_api_clear_method_handlers(FlBinaryMessenger* messenger, const gchar* suffix) {
  g_autofree gchar* dot_suffix = suffix != nullptr ? g_strdup_printf(".%s", suffix) : g_strdup("");

  g_autoptr(PigeonRunnerMessageCodec) codec = pigeon_runner_message_codec_new();
  g_autofree gchar* get_system_info_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.getSystemInfo%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) get_system_info_channel = fl_basic_message_channel_new(messenger, get_system_info_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(get_system_info_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_window_title_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.setWindowTitle%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_window_title_channel = fl_basic_message_channel_new(messenger, set_window_title_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_window_title_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_fullscreen_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.setFullscreen%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_fullscreen_channel = fl_basic_message_channel_new(messenger, set_fullscreen_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_fullscreen_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* set_maximized_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.setMaximized%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) set_maximized_channel = fl_basic_message_channel_new(messenger, set_maximized_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(set_maximized_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* minimize_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.minimize%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) minimize_channel = fl_basic_message_channel_new(messenger, minimize_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(minimize_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* ping_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostApi.ping%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) ping_channel = fl_basic_message_channel_new(messenger, ping_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(ping_channel, nullptr, nullptr, nullptr);
}

void pigeon_runner_runner_host_api_respond_get_system_info(PigeonRunnerRunnerHostApiResponseHandle* response_handle, PigeonRunnerRunnerSystemInfoData* return_value) {
  g_autoptr(PigeonRunnerRunnerHostApiGetSystemInfoResponse) response = pigeon_runner_runner_host_api_get_system_info_response_new(return_value);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "getSystemInfo", error->message);
  }
}

void pigeon_runner_runner_host_api_respond_error_get_system_info(PigeonRunnerRunnerHostApiResponseHandle* response_handle, const gchar* code, const gchar* message, FlValue* details) {
  g_autoptr(PigeonRunnerRunnerHostApiGetSystemInfoResponse) response = pigeon_runner_runner_host_api_get_system_info_response_new_error(code, message, details);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "getSystemInfo", error->message);
  }
}

void pigeon_runner_runner_host_api_respond_set_window_title(PigeonRunnerRunnerHostApiResponseHandle* response_handle, gboolean return_value) {
  g_autoptr(PigeonRunnerRunnerHostApiSetWindowTitleResponse) response = pigeon_runner_runner_host_api_set_window_title_response_new(return_value);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "setWindowTitle", error->message);
  }
}

void pigeon_runner_runner_host_api_respond_error_set_window_title(PigeonRunnerRunnerHostApiResponseHandle* response_handle, const gchar* code, const gchar* message, FlValue* details) {
  g_autoptr(PigeonRunnerRunnerHostApiSetWindowTitleResponse) response = pigeon_runner_runner_host_api_set_window_title_response_new_error(code, message, details);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "setWindowTitle", error->message);
  }
}

void pigeon_runner_runner_host_api_respond_set_fullscreen(PigeonRunnerRunnerHostApiResponseHandle* response_handle, gboolean return_value) {
  g_autoptr(PigeonRunnerRunnerHostApiSetFullscreenResponse) response = pigeon_runner_runner_host_api_set_fullscreen_response_new(return_value);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "setFullscreen", error->message);
  }
}

void pigeon_runner_runner_host_api_respond_error_set_fullscreen(PigeonRunnerRunnerHostApiResponseHandle* response_handle, const gchar* code, const gchar* message, FlValue* details) {
  g_autoptr(PigeonRunnerRunnerHostApiSetFullscreenResponse) response = pigeon_runner_runner_host_api_set_fullscreen_response_new_error(code, message, details);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "setFullscreen", error->message);
  }
}

void pigeon_runner_runner_host_api_respond_set_maximized(PigeonRunnerRunnerHostApiResponseHandle* response_handle, gboolean return_value) {
  g_autoptr(PigeonRunnerRunnerHostApiSetMaximizedResponse) response = pigeon_runner_runner_host_api_set_maximized_response_new(return_value);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "setMaximized", error->message);
  }
}

void pigeon_runner_runner_host_api_respond_error_set_maximized(PigeonRunnerRunnerHostApiResponseHandle* response_handle, const gchar* code, const gchar* message, FlValue* details) {
  g_autoptr(PigeonRunnerRunnerHostApiSetMaximizedResponse) response = pigeon_runner_runner_host_api_set_maximized_response_new_error(code, message, details);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "setMaximized", error->message);
  }
}

void pigeon_runner_runner_host_api_respond_minimize(PigeonRunnerRunnerHostApiResponseHandle* response_handle, gboolean return_value) {
  g_autoptr(PigeonRunnerRunnerHostApiMinimizeResponse) response = pigeon_runner_runner_host_api_minimize_response_new(return_value);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "minimize", error->message);
  }
}

void pigeon_runner_runner_host_api_respond_error_minimize(PigeonRunnerRunnerHostApiResponseHandle* response_handle, const gchar* code, const gchar* message, FlValue* details) {
  g_autoptr(PigeonRunnerRunnerHostApiMinimizeResponse) response = pigeon_runner_runner_host_api_minimize_response_new_error(code, message, details);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "minimize", error->message);
  }
}

void pigeon_runner_runner_host_api_respond_ping(PigeonRunnerRunnerHostApiResponseHandle* response_handle, PigeonRunnerRunnerPongData* return_value) {
  g_autoptr(PigeonRunnerRunnerHostApiPingResponse) response = pigeon_runner_runner_host_api_ping_response_new(return_value);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "ping", error->message);
  }
}

void pigeon_runner_runner_host_api_respond_error_ping(PigeonRunnerRunnerHostApiResponseHandle* response_handle, const gchar* code, const gchar* message, FlValue* details) {
  g_autoptr(PigeonRunnerRunnerHostApiPingResponse) response = pigeon_runner_runner_host_api_ping_response_new_error(code, message, details);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostApi", "ping", error->message);
  }
}

struct _PigeonRunnerRunnerHostIpcApiResponseHandle {
  GObject parent_instance;

  FlBasicMessageChannel* channel;
  FlBasicMessageChannelResponseHandle* response_handle;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostIpcApiResponseHandle, pigeon_runner_runner_host_ipc_api_response_handle, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_ipc_api_response_handle_dispose(GObject* object) {
  PigeonRunnerRunnerHostIpcApiResponseHandle* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_RESPONSE_HANDLE(object);
  g_clear_object(&self->channel);
  g_clear_object(&self->response_handle);
  G_OBJECT_CLASS(pigeon_runner_runner_host_ipc_api_response_handle_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_ipc_api_response_handle_init(PigeonRunnerRunnerHostIpcApiResponseHandle* self) {
}

static void pigeon_runner_runner_host_ipc_api_response_handle_class_init(PigeonRunnerRunnerHostIpcApiResponseHandleClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_ipc_api_response_handle_dispose;
}

static PigeonRunnerRunnerHostIpcApiResponseHandle* pigeon_runner_runner_host_ipc_api_response_handle_new(FlBasicMessageChannel* channel, FlBasicMessageChannelResponseHandle* response_handle) {
  PigeonRunnerRunnerHostIpcApiResponseHandle* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_RESPONSE_HANDLE(g_object_new(pigeon_runner_runner_host_ipc_api_response_handle_get_type(), nullptr));
  self->channel = FL_BASIC_MESSAGE_CHANNEL(g_object_ref(channel));
  self->response_handle = FL_BASIC_MESSAGE_CHANNEL_RESPONSE_HANDLE(g_object_ref(response_handle));
  return self;
}

G_DECLARE_FINAL_TYPE(PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponse, pigeon_runner_runner_host_ipc_api_get_compositor_info_response, PIGEON_RUNNER, RUNNER_HOST_IPC_API_GET_COMPOSITOR_INFO_RESPONSE, GObject)

struct _PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponse, pigeon_runner_runner_host_ipc_api_get_compositor_info_response, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_ipc_api_get_compositor_info_response_dispose(GObject* object) {
  PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_GET_COMPOSITOR_INFO_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(pigeon_runner_runner_host_ipc_api_get_compositor_info_response_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_ipc_api_get_compositor_info_response_init(PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponse* self) {
}

static void pigeon_runner_runner_host_ipc_api_get_compositor_info_response_class_init(PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_ipc_api_get_compositor_info_response_dispose;
}

static PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponse* pigeon_runner_runner_host_ipc_api_get_compositor_info_response_new(PigeonRunnerCompositorSystemInfoData* return_value) {
  PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_GET_COMPOSITOR_INFO_RESPONSE(g_object_new(pigeon_runner_runner_host_ipc_api_get_compositor_info_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, return_value != nullptr ? fl_value_new_custom_object(pigeon_runner_compositor_system_info_data_type_id, G_OBJECT(return_value)) : fl_value_new_null());
  return self;
}

static PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponse* pigeon_runner_runner_host_ipc_api_get_compositor_info_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_GET_COMPOSITOR_INFO_RESPONSE(g_object_new(pigeon_runner_runner_host_ipc_api_get_compositor_info_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

G_DECLARE_FINAL_TYPE(PigeonRunnerRunnerHostIpcApiPingCompositorResponse, pigeon_runner_runner_host_ipc_api_ping_compositor_response, PIGEON_RUNNER, RUNNER_HOST_IPC_API_PING_COMPOSITOR_RESPONSE, GObject)

struct _PigeonRunnerRunnerHostIpcApiPingCompositorResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostIpcApiPingCompositorResponse, pigeon_runner_runner_host_ipc_api_ping_compositor_response, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_ipc_api_ping_compositor_response_dispose(GObject* object) {
  PigeonRunnerRunnerHostIpcApiPingCompositorResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_PING_COMPOSITOR_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(pigeon_runner_runner_host_ipc_api_ping_compositor_response_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_ipc_api_ping_compositor_response_init(PigeonRunnerRunnerHostIpcApiPingCompositorResponse* self) {
}

static void pigeon_runner_runner_host_ipc_api_ping_compositor_response_class_init(PigeonRunnerRunnerHostIpcApiPingCompositorResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_ipc_api_ping_compositor_response_dispose;
}

static PigeonRunnerRunnerHostIpcApiPingCompositorResponse* pigeon_runner_runner_host_ipc_api_ping_compositor_response_new(PigeonRunnerCompositorPongData* return_value) {
  PigeonRunnerRunnerHostIpcApiPingCompositorResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_PING_COMPOSITOR_RESPONSE(g_object_new(pigeon_runner_runner_host_ipc_api_ping_compositor_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, return_value != nullptr ? fl_value_new_custom_object(pigeon_runner_compositor_pong_data_type_id, G_OBJECT(return_value)) : fl_value_new_null());
  return self;
}

static PigeonRunnerRunnerHostIpcApiPingCompositorResponse* pigeon_runner_runner_host_ipc_api_ping_compositor_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  PigeonRunnerRunnerHostIpcApiPingCompositorResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_PING_COMPOSITOR_RESPONSE(g_object_new(pigeon_runner_runner_host_ipc_api_ping_compositor_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

G_DECLARE_FINAL_TYPE(PigeonRunnerRunnerHostIpcApiListSurfacesResponse, pigeon_runner_runner_host_ipc_api_list_surfaces_response, PIGEON_RUNNER, RUNNER_HOST_IPC_API_LIST_SURFACES_RESPONSE, GObject)

struct _PigeonRunnerRunnerHostIpcApiListSurfacesResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostIpcApiListSurfacesResponse, pigeon_runner_runner_host_ipc_api_list_surfaces_response, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_ipc_api_list_surfaces_response_dispose(GObject* object) {
  PigeonRunnerRunnerHostIpcApiListSurfacesResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_LIST_SURFACES_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(pigeon_runner_runner_host_ipc_api_list_surfaces_response_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_ipc_api_list_surfaces_response_init(PigeonRunnerRunnerHostIpcApiListSurfacesResponse* self) {
}

static void pigeon_runner_runner_host_ipc_api_list_surfaces_response_class_init(PigeonRunnerRunnerHostIpcApiListSurfacesResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_ipc_api_list_surfaces_response_dispose;
}

static PigeonRunnerRunnerHostIpcApiListSurfacesResponse* pigeon_runner_runner_host_ipc_api_list_surfaces_response_new(FlValue* return_value) {
  PigeonRunnerRunnerHostIpcApiListSurfacesResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_LIST_SURFACES_RESPONSE(g_object_new(pigeon_runner_runner_host_ipc_api_list_surfaces_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, return_value != nullptr ? fl_value_ref(return_value) : fl_value_new_null());
  return self;
}

static PigeonRunnerRunnerHostIpcApiListSurfacesResponse* pigeon_runner_runner_host_ipc_api_list_surfaces_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  PigeonRunnerRunnerHostIpcApiListSurfacesResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_LIST_SURFACES_RESPONSE(g_object_new(pigeon_runner_runner_host_ipc_api_list_surfaces_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

G_DECLARE_FINAL_TYPE(PigeonRunnerRunnerHostIpcApiCloseSurfaceResponse, pigeon_runner_runner_host_ipc_api_close_surface_response, PIGEON_RUNNER, RUNNER_HOST_IPC_API_CLOSE_SURFACE_RESPONSE, GObject)

struct _PigeonRunnerRunnerHostIpcApiCloseSurfaceResponse {
  GObject parent_instance;

  FlValue* value;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostIpcApiCloseSurfaceResponse, pigeon_runner_runner_host_ipc_api_close_surface_response, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_ipc_api_close_surface_response_dispose(GObject* object) {
  PigeonRunnerRunnerHostIpcApiCloseSurfaceResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_CLOSE_SURFACE_RESPONSE(object);
  g_clear_pointer(&self->value, fl_value_unref);
  G_OBJECT_CLASS(pigeon_runner_runner_host_ipc_api_close_surface_response_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_ipc_api_close_surface_response_init(PigeonRunnerRunnerHostIpcApiCloseSurfaceResponse* self) {
}

static void pigeon_runner_runner_host_ipc_api_close_surface_response_class_init(PigeonRunnerRunnerHostIpcApiCloseSurfaceResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_ipc_api_close_surface_response_dispose;
}

static PigeonRunnerRunnerHostIpcApiCloseSurfaceResponse* pigeon_runner_runner_host_ipc_api_close_surface_response_new(gboolean return_value) {
  PigeonRunnerRunnerHostIpcApiCloseSurfaceResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_CLOSE_SURFACE_RESPONSE(g_object_new(pigeon_runner_runner_host_ipc_api_close_surface_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_bool(return_value));
  return self;
}

static PigeonRunnerRunnerHostIpcApiCloseSurfaceResponse* pigeon_runner_runner_host_ipc_api_close_surface_response_new_error(const gchar* code, const gchar* message, FlValue* details) {
  PigeonRunnerRunnerHostIpcApiCloseSurfaceResponse* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API_CLOSE_SURFACE_RESPONSE(g_object_new(pigeon_runner_runner_host_ipc_api_close_surface_response_get_type(), nullptr));
  self->value = fl_value_new_list();
  fl_value_append_take(self->value, fl_value_new_string(code));
  fl_value_append_take(self->value, fl_value_new_string(message != nullptr ? message : ""));
  fl_value_append_take(self->value, details != nullptr ? fl_value_ref(details) : fl_value_new_null());
  return self;
}

struct _PigeonRunnerRunnerHostIpcApi {
  GObject parent_instance;

  const PigeonRunnerRunnerHostIpcApiVTable* vtable;
  gpointer user_data;
  GDestroyNotify user_data_free_func;
};

G_DEFINE_TYPE(PigeonRunnerRunnerHostIpcApi, pigeon_runner_runner_host_ipc_api, G_TYPE_OBJECT)

static void pigeon_runner_runner_host_ipc_api_dispose(GObject* object) {
  PigeonRunnerRunnerHostIpcApi* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API(object);
  if (self->user_data != nullptr) {
    self->user_data_free_func(self->user_data);
  }
  self->user_data = nullptr;
  G_OBJECT_CLASS(pigeon_runner_runner_host_ipc_api_parent_class)->dispose(object);
}

static void pigeon_runner_runner_host_ipc_api_init(PigeonRunnerRunnerHostIpcApi* self) {
}

static void pigeon_runner_runner_host_ipc_api_class_init(PigeonRunnerRunnerHostIpcApiClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_host_ipc_api_dispose;
}

static PigeonRunnerRunnerHostIpcApi* pigeon_runner_runner_host_ipc_api_new(const PigeonRunnerRunnerHostIpcApiVTable* vtable, gpointer user_data, GDestroyNotify user_data_free_func) {
  PigeonRunnerRunnerHostIpcApi* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API(g_object_new(pigeon_runner_runner_host_ipc_api_get_type(), nullptr));
  self->vtable = vtable;
  self->user_data = user_data;
  self->user_data_free_func = user_data_free_func;
  return self;
}

static void pigeon_runner_runner_host_ipc_api_get_compositor_info_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  PigeonRunnerRunnerHostIpcApi* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API(user_data);

  if (self->vtable == nullptr || self->vtable->get_compositor_info == nullptr) {
    return;
  }

  g_autoptr(PigeonRunnerRunnerHostIpcApiResponseHandle) handle = pigeon_runner_runner_host_ipc_api_response_handle_new(channel, response_handle);
  self->vtable->get_compositor_info(handle, self->user_data);
}

static void pigeon_runner_runner_host_ipc_api_ping_compositor_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  PigeonRunnerRunnerHostIpcApi* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API(user_data);

  if (self->vtable == nullptr || self->vtable->ping_compositor == nullptr) {
    return;
  }

  g_autoptr(PigeonRunnerRunnerHostIpcApiResponseHandle) handle = pigeon_runner_runner_host_ipc_api_response_handle_new(channel, response_handle);
  self->vtable->ping_compositor(handle, self->user_data);
}

static void pigeon_runner_runner_host_ipc_api_list_surfaces_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  PigeonRunnerRunnerHostIpcApi* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API(user_data);

  if (self->vtable == nullptr || self->vtable->list_surfaces == nullptr) {
    return;
  }

  g_autoptr(PigeonRunnerRunnerHostIpcApiResponseHandle) handle = pigeon_runner_runner_host_ipc_api_response_handle_new(channel, response_handle);
  self->vtable->list_surfaces(handle, self->user_data);
}

static void pigeon_runner_runner_host_ipc_api_close_surface_cb(FlBasicMessageChannel* channel, FlValue* message_, FlBasicMessageChannelResponseHandle* response_handle, gpointer user_data) {
  PigeonRunnerRunnerHostIpcApi* self = PIGEON_RUNNER_RUNNER_HOST_IPC_API(user_data);

  if (self->vtable == nullptr || self->vtable->close_surface == nullptr) {
    return;
  }

  FlValue* value0 = fl_value_get_list_value(message_, 0);
  int64_t surface_handle = fl_value_get_int(value0);
  g_autoptr(PigeonRunnerRunnerHostIpcApiResponseHandle) handle = pigeon_runner_runner_host_ipc_api_response_handle_new(channel, response_handle);
  self->vtable->close_surface(surface_handle, handle, self->user_data);
}

void pigeon_runner_runner_host_ipc_api_set_method_handlers(FlBinaryMessenger* messenger, const gchar* suffix, const PigeonRunnerRunnerHostIpcApiVTable* vtable, gpointer user_data, GDestroyNotify user_data_free_func) {
  g_autofree gchar* dot_suffix = suffix != nullptr ? g_strdup_printf(".%s", suffix) : g_strdup("");
  g_autoptr(PigeonRunnerRunnerHostIpcApi) api_data = pigeon_runner_runner_host_ipc_api_new(vtable, user_data, user_data_free_func);

  g_autoptr(PigeonRunnerMessageCodec) codec = pigeon_runner_message_codec_new();
  g_autofree gchar* get_compositor_info_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostIpcApi.getCompositorInfo%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) get_compositor_info_channel = fl_basic_message_channel_new(messenger, get_compositor_info_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(get_compositor_info_channel, pigeon_runner_runner_host_ipc_api_get_compositor_info_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* ping_compositor_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostIpcApi.pingCompositor%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) ping_compositor_channel = fl_basic_message_channel_new(messenger, ping_compositor_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(ping_compositor_channel, pigeon_runner_runner_host_ipc_api_ping_compositor_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* list_surfaces_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostIpcApi.listSurfaces%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) list_surfaces_channel = fl_basic_message_channel_new(messenger, list_surfaces_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(list_surfaces_channel, pigeon_runner_runner_host_ipc_api_list_surfaces_cb, g_object_ref(api_data), g_object_unref);
  g_autofree gchar* close_surface_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostIpcApi.closeSurface%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) close_surface_channel = fl_basic_message_channel_new(messenger, close_surface_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(close_surface_channel, pigeon_runner_runner_host_ipc_api_close_surface_cb, g_object_ref(api_data), g_object_unref);
}

void pigeon_runner_runner_host_ipc_api_clear_method_handlers(FlBinaryMessenger* messenger, const gchar* suffix) {
  g_autofree gchar* dot_suffix = suffix != nullptr ? g_strdup_printf(".%s", suffix) : g_strdup("");

  g_autoptr(PigeonRunnerMessageCodec) codec = pigeon_runner_message_codec_new();
  g_autofree gchar* get_compositor_info_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostIpcApi.getCompositorInfo%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) get_compositor_info_channel = fl_basic_message_channel_new(messenger, get_compositor_info_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(get_compositor_info_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* ping_compositor_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostIpcApi.pingCompositor%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) ping_compositor_channel = fl_basic_message_channel_new(messenger, ping_compositor_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(ping_compositor_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* list_surfaces_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostIpcApi.listSurfaces%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) list_surfaces_channel = fl_basic_message_channel_new(messenger, list_surfaces_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(list_surfaces_channel, nullptr, nullptr, nullptr);
  g_autofree gchar* close_surface_channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerHostIpcApi.closeSurface%s", dot_suffix);
  g_autoptr(FlBasicMessageChannel) close_surface_channel = fl_basic_message_channel_new(messenger, close_surface_channel_name, FL_MESSAGE_CODEC(codec));
  fl_basic_message_channel_set_message_handler(close_surface_channel, nullptr, nullptr, nullptr);
}

void pigeon_runner_runner_host_ipc_api_respond_get_compositor_info(PigeonRunnerRunnerHostIpcApiResponseHandle* response_handle, PigeonRunnerCompositorSystemInfoData* return_value) {
  g_autoptr(PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponse) response = pigeon_runner_runner_host_ipc_api_get_compositor_info_response_new(return_value);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostIpcApi", "getCompositorInfo", error->message);
  }
}

void pigeon_runner_runner_host_ipc_api_respond_error_get_compositor_info(PigeonRunnerRunnerHostIpcApiResponseHandle* response_handle, const gchar* code, const gchar* message, FlValue* details) {
  g_autoptr(PigeonRunnerRunnerHostIpcApiGetCompositorInfoResponse) response = pigeon_runner_runner_host_ipc_api_get_compositor_info_response_new_error(code, message, details);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostIpcApi", "getCompositorInfo", error->message);
  }
}

void pigeon_runner_runner_host_ipc_api_respond_ping_compositor(PigeonRunnerRunnerHostIpcApiResponseHandle* response_handle, PigeonRunnerCompositorPongData* return_value) {
  g_autoptr(PigeonRunnerRunnerHostIpcApiPingCompositorResponse) response = pigeon_runner_runner_host_ipc_api_ping_compositor_response_new(return_value);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostIpcApi", "pingCompositor", error->message);
  }
}

void pigeon_runner_runner_host_ipc_api_respond_error_ping_compositor(PigeonRunnerRunnerHostIpcApiResponseHandle* response_handle, const gchar* code, const gchar* message, FlValue* details) {
  g_autoptr(PigeonRunnerRunnerHostIpcApiPingCompositorResponse) response = pigeon_runner_runner_host_ipc_api_ping_compositor_response_new_error(code, message, details);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostIpcApi", "pingCompositor", error->message);
  }
}

void pigeon_runner_runner_host_ipc_api_respond_list_surfaces(PigeonRunnerRunnerHostIpcApiResponseHandle* response_handle, FlValue* return_value) {
  g_autoptr(PigeonRunnerRunnerHostIpcApiListSurfacesResponse) response = pigeon_runner_runner_host_ipc_api_list_surfaces_response_new(return_value);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostIpcApi", "listSurfaces", error->message);
  }
}

void pigeon_runner_runner_host_ipc_api_respond_error_list_surfaces(PigeonRunnerRunnerHostIpcApiResponseHandle* response_handle, const gchar* code, const gchar* message, FlValue* details) {
  g_autoptr(PigeonRunnerRunnerHostIpcApiListSurfacesResponse) response = pigeon_runner_runner_host_ipc_api_list_surfaces_response_new_error(code, message, details);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostIpcApi", "listSurfaces", error->message);
  }
}

void pigeon_runner_runner_host_ipc_api_respond_close_surface(PigeonRunnerRunnerHostIpcApiResponseHandle* response_handle, gboolean return_value) {
  g_autoptr(PigeonRunnerRunnerHostIpcApiCloseSurfaceResponse) response = pigeon_runner_runner_host_ipc_api_close_surface_response_new(return_value);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostIpcApi", "closeSurface", error->message);
  }
}

void pigeon_runner_runner_host_ipc_api_respond_error_close_surface(PigeonRunnerRunnerHostIpcApiResponseHandle* response_handle, const gchar* code, const gchar* message, FlValue* details) {
  g_autoptr(PigeonRunnerRunnerHostIpcApiCloseSurfaceResponse) response = pigeon_runner_runner_host_ipc_api_close_surface_response_new_error(code, message, details);
  g_autoptr(GError) error = nullptr;
  if (!fl_basic_message_channel_respond(response_handle->channel, response_handle->response_handle, response->value, &error)) {
    g_warning("Failed to send response to %s.%s: %s", "RunnerHostIpcApi", "closeSurface", error->message);
  }
}

struct _PigeonRunnerRunnerFlutterIpcApi {
  GObject parent_instance;

  FlBinaryMessenger* messenger;
  gchar *suffix;
};

G_DEFINE_TYPE(PigeonRunnerRunnerFlutterIpcApi, pigeon_runner_runner_flutter_ipc_api, G_TYPE_OBJECT)

static void pigeon_runner_runner_flutter_ipc_api_dispose(GObject* object) {
  PigeonRunnerRunnerFlutterIpcApi* self = PIGEON_RUNNER_RUNNER_FLUTTER_IPC_API(object);
  g_clear_object(&self->messenger);
  g_clear_pointer(&self->suffix, g_free);
  G_OBJECT_CLASS(pigeon_runner_runner_flutter_ipc_api_parent_class)->dispose(object);
}

static void pigeon_runner_runner_flutter_ipc_api_init(PigeonRunnerRunnerFlutterIpcApi* self) {
}

static void pigeon_runner_runner_flutter_ipc_api_class_init(PigeonRunnerRunnerFlutterIpcApiClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_flutter_ipc_api_dispose;
}

PigeonRunnerRunnerFlutterIpcApi* pigeon_runner_runner_flutter_ipc_api_new(FlBinaryMessenger* messenger, const gchar* suffix) {
  PigeonRunnerRunnerFlutterIpcApi* self = PIGEON_RUNNER_RUNNER_FLUTTER_IPC_API(g_object_new(pigeon_runner_runner_flutter_ipc_api_get_type(), nullptr));
  self->messenger = FL_BINARY_MESSENGER(g_object_ref(messenger));
  self->suffix = suffix != nullptr ? g_strdup_printf(".%s", suffix) : g_strdup("");
  return self;
}

struct _PigeonRunnerRunnerFlutterIpcApiOnNotificationResponse {
  GObject parent_instance;

  FlValue* error;
};

G_DEFINE_TYPE(PigeonRunnerRunnerFlutterIpcApiOnNotificationResponse, pigeon_runner_runner_flutter_ipc_api_on_notification_response, G_TYPE_OBJECT)

static void pigeon_runner_runner_flutter_ipc_api_on_notification_response_dispose(GObject* object) {
  PigeonRunnerRunnerFlutterIpcApiOnNotificationResponse* self = PIGEON_RUNNER_RUNNER_FLUTTER_IPC_API_ON_NOTIFICATION_RESPONSE(object);
  g_clear_pointer(&self->error, fl_value_unref);
  G_OBJECT_CLASS(pigeon_runner_runner_flutter_ipc_api_on_notification_response_parent_class)->dispose(object);
}

static void pigeon_runner_runner_flutter_ipc_api_on_notification_response_init(PigeonRunnerRunnerFlutterIpcApiOnNotificationResponse* self) {
}

static void pigeon_runner_runner_flutter_ipc_api_on_notification_response_class_init(PigeonRunnerRunnerFlutterIpcApiOnNotificationResponseClass* klass) {
  G_OBJECT_CLASS(klass)->dispose = pigeon_runner_runner_flutter_ipc_api_on_notification_response_dispose;
}

static PigeonRunnerRunnerFlutterIpcApiOnNotificationResponse* pigeon_runner_runner_flutter_ipc_api_on_notification_response_new(FlValue* response) {
  PigeonRunnerRunnerFlutterIpcApiOnNotificationResponse* self = PIGEON_RUNNER_RUNNER_FLUTTER_IPC_API_ON_NOTIFICATION_RESPONSE(g_object_new(pigeon_runner_runner_flutter_ipc_api_on_notification_response_get_type(), nullptr));
  if (fl_value_get_length(response) > 1) {
    self->error = fl_value_ref(response);
  }
  return self;
}

gboolean pigeon_runner_runner_flutter_ipc_api_on_notification_response_is_error(PigeonRunnerRunnerFlutterIpcApiOnNotificationResponse* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_FLUTTER_IPC_API_ON_NOTIFICATION_RESPONSE(self), FALSE);
  return self->error != nullptr;
}

const gchar* pigeon_runner_runner_flutter_ipc_api_on_notification_response_get_error_code(PigeonRunnerRunnerFlutterIpcApiOnNotificationResponse* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_FLUTTER_IPC_API_ON_NOTIFICATION_RESPONSE(self), nullptr);
  g_assert(pigeon_runner_runner_flutter_ipc_api_on_notification_response_is_error(self));
  return fl_value_get_string(fl_value_get_list_value(self->error, 0));
}

const gchar* pigeon_runner_runner_flutter_ipc_api_on_notification_response_get_error_message(PigeonRunnerRunnerFlutterIpcApiOnNotificationResponse* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_FLUTTER_IPC_API_ON_NOTIFICATION_RESPONSE(self), nullptr);
  g_assert(pigeon_runner_runner_flutter_ipc_api_on_notification_response_is_error(self));
  return fl_value_get_string(fl_value_get_list_value(self->error, 1));
}

FlValue* pigeon_runner_runner_flutter_ipc_api_on_notification_response_get_error_details(PigeonRunnerRunnerFlutterIpcApiOnNotificationResponse* self) {
  g_return_val_if_fail(PIGEON_RUNNER_IS_RUNNER_FLUTTER_IPC_API_ON_NOTIFICATION_RESPONSE(self), nullptr);
  g_assert(pigeon_runner_runner_flutter_ipc_api_on_notification_response_is_error(self));
  return fl_value_get_list_value(self->error, 2);
}

static void pigeon_runner_runner_flutter_ipc_api_on_notification_cb(GObject* object, GAsyncResult* result, gpointer user_data) {
  GTask* task = G_TASK(user_data);
  g_task_return_pointer(task, result, g_object_unref);
}

void pigeon_runner_runner_flutter_ipc_api_on_notification(PigeonRunnerRunnerFlutterIpcApi* self, const gchar* method, const gchar* params_json, GCancellable* cancellable, GAsyncReadyCallback callback, gpointer user_data) {
  g_autoptr(FlValue) args = fl_value_new_list();
  fl_value_append_take(args, fl_value_new_string(method));
  fl_value_append_take(args, params_json != nullptr ? fl_value_new_string(params_json) : fl_value_new_null());
  g_autofree gchar* channel_name = g_strdup_printf("dev.flutter.pigeon.pigeon_runner.RunnerFlutterIpcApi.onNotification%s", self->suffix);
  g_autoptr(PigeonRunnerMessageCodec) codec = pigeon_runner_message_codec_new();
  FlBasicMessageChannel* channel = fl_basic_message_channel_new(self->messenger, channel_name, FL_MESSAGE_CODEC(codec));
  GTask* task = g_task_new(self, cancellable, callback, user_data);
  g_task_set_task_data(task, channel, g_object_unref);
  fl_basic_message_channel_send(channel, args, cancellable, pigeon_runner_runner_flutter_ipc_api_on_notification_cb, task);
}

PigeonRunnerRunnerFlutterIpcApiOnNotificationResponse* pigeon_runner_runner_flutter_ipc_api_on_notification_finish(PigeonRunnerRunnerFlutterIpcApi* self, GAsyncResult* result, GError** error) {
  g_autoptr(GTask) task = G_TASK(result);
  GAsyncResult* r = G_ASYNC_RESULT(g_task_propagate_pointer(task, nullptr));
  FlBasicMessageChannel* channel = FL_BASIC_MESSAGE_CHANNEL(g_task_get_task_data(task));
  g_autoptr(FlValue) response = fl_basic_message_channel_send_finish(channel, r, error);
  if (response == nullptr) { 
    return nullptr;
  }
  return pigeon_runner_runner_flutter_ipc_api_on_notification_response_new(response);
}
